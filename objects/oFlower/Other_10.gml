/// @desc

//Add the flower
flower_collect(flowerType);

//Store that I was eaten
game_persistence_set(storename, 1);
					//Should replace this with time
					//So it respawns after a while
	

//Destroy
instance_destroy();
